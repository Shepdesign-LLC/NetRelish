// PantryIntents.swift — the framework's App Intents package. The app includes it,
// so Shortcuts discovers the intents that live here.

import AppIntents

public struct PantryIntents: AppIntentsPackage {}
